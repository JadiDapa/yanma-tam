import { DocumentationPeriod } from "@/generated/prisma";
import DeviceCard from "./DeviceCard";
import CreateDeviceDialog from "./CreateDeviceDialog";

export default function DeviceList({
  devices,
  period,
}: {
  devices: DocumentationPeriod[];
  period: string;
}) {
  return (

  );
}
